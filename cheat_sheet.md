`git init` <br>
`git add . `<br>
`git origin add `<br>


// set ssh key for easy loggin
`$ssh-keygen`
`$cat ~/.bashrc | pcoby`
`$ssh -T git@github.com`




//change repo name

git clone 
git clone <url>
